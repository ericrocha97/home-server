HOME_SERVER_ENV=lab
HOME_SERVER_SSH_USER=ubuntu
ANSIBLE_VAULT_PASSWORD_FILE=
# Todas as senhas e N8N_ENCRYPTION_KEY ficam em ansible/inventories/<env>/group_vars/vault.yml (Ansible Vault)
# Compose .env são gerados pelo Ansible em /srv/home-server/compose/<service>/.env
