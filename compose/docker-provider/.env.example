# Sanitized example — Ansible renders the real .env on the host (0600, ignored).
# No secrets in this file; the proxy needs no credentials (read-only GET contract).
DOCKER_SOCKET_PROXY_IMAGE=tecnativa/docker-socket-proxy:0.3.0
DOCKER_SOCKET_PROXY_BIND_IP=192.0.2.10
