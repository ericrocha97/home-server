# Sanitized example — Ansible renders the real .env on the host (0600, ignored).
# AGENT_SECRET comes from Vault `portainer_agent_secret`; never commit the real value.
PORTAINER_AGENT_IMAGE=portainer/agent:2.19.5
PORTAINER_AGENT_BIND_IP=192.0.2.10
AGENT_SECRET=changeme-portainer-agent
