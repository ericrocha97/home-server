---
- name: Install CasaOS
  hosts: server
  become: yes
  vars:
    casaos_install_url: "https://get.icewhale.io/casaos.sh"

  tasks:
    - name: Download and run CasaOS installer
      shell: curl -fsSL "{{ casaos_install_url }}" | bash
      args:
        creates: /opt/casaos